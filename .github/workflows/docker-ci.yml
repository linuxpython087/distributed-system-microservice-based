# name: Build and Push Docker Image

# on:
#   push:
#     branches: [ "main", "master" ]  # support both (important fix)

# jobs:
#   # ---------------- CI ----------------
#   build:
#     runs-on: ubuntu-latest

#     steps:
#     # 1. Checkout code
#     - name: Checkout code
#       uses: actions/checkout@v4

#     # 2. Setup Docker Buildx
#     - name: Set up Docker Buildx
#       uses: docker/setup-buildx-action@v3

#     # 3. Login to Docker Hub
#     - name: Login to Docker Hub
#       uses: docker/login-action@v3
#       with:
#         username: ${{ secrets.DOCKERHUB_USERNAME }}
#         password: ${{ secrets.DOCKERHUB_TOKEN }}

#     # 4. Extract metadata (FIXED)
#     - name: Extract metadata (tags)
#       id: meta
#       uses: docker/metadata-action@v5
#       with:
#         images: ${{ secrets.DOCKERHUB_USERNAME }}/order-service
#         tags: |
          
#           type=v3.0.3

#     - name: Set up QEMU
#       uses: docker/setup-qemu-action@v4

#     # 5. buildx
#     - name: Buildx
#       uses: docker/setup-buildx-action@v4
    

#     # 5. Build & Push image
#     - name: Build and Push
#       uses: docker/build-push-action@v7
#       with:
#         context: .
#         push: true
#         tags: ${{ steps.meta.outputs.tags }}
#         labels: ${{ steps.meta.outputs.labels }}




#   # ---------------- CD ----------------

#   deploy:
#     runs-on: ubuntu-latest
#     needs: build

#     steps:
#     # 1. Setup kubectl
#     - name: Setup kubectl
#       uses: azure/setup-kubectl@v4

#     # 2. Configure kubeconfig
#     - name: Configure kubeconfig
#       run: |
#         mkdir -p ~/.kube
#         echo "${{ secrets.KUBE_CONFIG }}" > ~/.kube/config

#     # 3. Define image (important for consistency)
#     - name: Set image variable
#       run: |
#         echo "IMAGE=${{ secrets.DOCKERHUB_USERNAME }}/tchalim-canonical-distributed:${{ github.sha }}" >> $GITHUB_ENV

#     # 4. Debug (VERY IMPORTANT)
#     - name: Debug image
#       run: |
#         echo "Deploying image: $IMAGE"

#     # 5. Deploy new version
#     - name: Update deployment image
#       run: |
#         kubectl set image deployment/fault-tolerant-api \
#         fault-tolerant=$IMAGE \
#         -n fault-tolerant

#     # 6. Wait for rollout (strict)
#     - name: Wait for rollout
#       run: |
#         kubectl rollout status deployment/fault-tolerant-api \
#         -n fault-tolerant \
#         --timeout=120s

#     # 7. Verify pods (extra safety)
#     - name: Verify pods
#       run: |
#         kubectl get pods -n fault-tolerant
#         kubectl describe deployment fault-tolerant-api -n fault-tolerant

#     # 8. Rollback if failure
#     - name: Rollback if failed
#       if: failure()
#       run: |
#         echo "Deployment failed. Rolling back..."
#         kubectl rollout undo deployment/fault-tolerant-api -n fault-tolerant
  



  

